-- Generated by orbit-theme. Do not edit manually.
local primary = "rgb(83a598)"
local surface = "rgb(282828)"
local secondary = "rgb(d3869b)"
local error = "rgb(fb4934)"

local function apply_theme()
    hl.config({
        general = { col = { active_border = primary, inactive_border = surface } },
        group = {
            col = {
                border_active = secondary,
                border_inactive = surface,
                border_locked_active = error,
                border_locked_inactive = surface,
            },
            groupbar = {
                col = {
                    active = secondary,
                    inactive = surface,
                    locked_active = error,
                    locked_inactive = surface,
                },
            },
        },
    })
end

return {
    colors = { primary = primary, surface = surface, secondary = secondary, error = error },
    apply_theme = apply_theme,
}
