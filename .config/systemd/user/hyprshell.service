[Unit]
Description=Hyprshell workspace switcher (disabled; Orbit owns the overview)
PartOf=hyprland-session.target
After=hyprland-session.target

[Service]
ExecStart=/usr/bin/true

[Install]
WantedBy=hyprland-session.target
