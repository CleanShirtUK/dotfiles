---
title: Orbit - Status
type: project-status
updated: 2026-08-17
tags: [orbit, status]
---

# Orbit Status

## Current Snapshot

- Stabilization is the current priority. A direct live audit on 2026-08-17 found no runtime `TAB` binding even though `orbit-shell.service` was active and the compositor signature matched the current Hyprland instance. `START-004` is `OPEN` and requires revalidation; earlier successful binding runs are historical, not current proof.
- Input-device authorization is `PASS`: the current udev-identity allowlist, fail-closed filtering, fresh-login service startup, and attended Alt+release behavior passed. The earlier ACL-only `OPEN` result is retained below as historical evidence of a reverted approach.
- `START-006` implementation and current automated fixtures pass, but the real attended cancel and explicitly approved logout/re-login path remain `MANUAL` in `VQ-20260817-16`.
- `orbit/project-manifest.json` is the canonical project and work-item source; this page is a human-readable view validated against it.
- Process-cleanup baseline: the canonical manifest validates with zero errors or migration warnings, all 50 registered deterministic contract IDs pass, required runtime files are allowlisted for Git, and generated appearance/window-rule artifacts match their canonical settings sources. Current live validation still fails only `START-004` because no runtime `TAB` binding is present.

## Recent Implemented Boundaries

- `ORB-SETTINGS-PYTHON-BOUNDARIES` artifact-generation boundary: `.local/lib/orbit_settings_artifacts.py` now owns window-rule, monitor, and appearance artifact serialization; `.local/bin/orbit-settings` preserves compatibility wrappers. Contract `SET-006` and the full contract suite pass; the sibling persistence, validation, observation, and action boundaries are also complete.
- `ORB-SETTINGS-PERSISTENCE-BOUNDARY` persistence boundary: `.local/lib/orbit_settings_persistence.py` now owns atomic replacement, TOML/JSON reads, and TOML scalar serialization; `.local/bin/orbit-settings` preserves its command surface and file formats. `SET-007` contract/live/soak checks pass; the sibling Settings Python boundaries are complete.
- `ORB-SETTINGS-VALIDATION-BOUNDARY` validation boundary: `.local/lib/orbit_settings_validation.py` now owns application-policy, appearance, and display-profile validation; `.local/bin/orbit-settings` preserves its command surface and rejection semantics. `SET-008` contract 32/32, live 36/36, and one-minute soak 10/10 pass; the sibling Settings Python boundaries are complete.
- `ORB-SETTINGS-RUNTIME-OBSERVATION` read-only runtime boundary: `.local/lib/orbit_settings_runtime.py` now owns audio, network, Bluetooth, and power snapshot observation; `.local/bin/orbit-settings` preserves the snapshot API and hypridle projection. `SET-009` contract 32/32, live 36/36, and one-minute soak 11/11 pass; mutating actions are separately owned by `.local/lib/orbit_settings_actions.py` under `SET-010`.
- `ORB-SETTINGS-ACTIONS-BOUNDARY` mutating Settings runtime boundary: `.local/lib/orbit_settings_actions.py` now owns transient audio, network, Bluetooth, power, and hypridle actions; `.local/bin/orbit-settings` preserves compatibility wrappers. `SET-010` contract passed 35/35; live passed 38/39 with unrelated `START-004` binding failure; one-minute soak passed 10/10. No real device or network mutation was performed.
- `ORB-APP-CLOSE-DISPATCH` application close boundary: dock and top-panel close actions now focus the selected client and use Hyprland 0.56.1's Lua close dispatcher; the disposable WezTerm close fixture passed. `UI-019` contract 35/35 (`2026-08-17T17-15-47Z-596641`), live 38/39 (`2026-08-17T17-15-51Z-597112`, unrelated `START-004` failure), and one-minute soak 11/11 (`soak-2026-08-17T17-15-54Z-597644`) passed. No unrelated window or session state was changed.
- `ORB-DOCK-MAGNIFICATION-EDGE` boundary-entry correction: dock hover amount now ramps over 140 ms through the same `scaleAt()` interpolation used for in-dock magnification. Contract 32/32 (`2026-08-17T16-37-50Z-333107`), live 36/36 after shell restart (`2026-08-17T16-39-49Z-347788`), and one-minute soak 11/11 pass; attended two-monitor slow-entry validation remains queued as `VQ-20260817-10`.
- `ORB-THEME-PROPAGATION` validation boundary: `THEME-003` now has deterministic coverage for installing GTK3, GTK4, and KDE active adapters from one generated palette. Contract 33/33 (`2026-08-17T16-49-39Z-415867`) and one-minute soak 11/11 (`soak-2026-08-17T16-49-48Z-417118`) pass; live reached 36/37 twice because unrelated `START-004` Alt+Tab binding validation failed while theme checks passed. Current selected palette is `catppuccin-mocha`; user-dirty active adapters were preserved, and attended reload/restart behavior remains `VQ-20260817-11`.
- `ORB-DISPLAY-RECOVERY` safety boundary: display role/profile applies now snapshot generated files and the observed Hyprland topology, verify requested dimensions, and restore both on any command, reload, or verification failure. Synthetic `SET-003` recovery and contract 34/34 pass; live reached 37/38 because unrelated `START-004` Alt+Tab binding validation failed, and one-minute soak passed 10/10. No real display mutation was performed; disposable two-monitor recovery remains `VQ-20260817-12`.

## Phase Summary

| Deliverable | State | Evidence / next gate |
| --- | --- | --- |
| Monitor role resolver | PASS | `.local/bin/orbit-monitor`, `MON-001` |
| Application policy resolver | PASS | `.local/bin/orbit-app-policy`, `APP-001` |
| Dynamic workspace routing | MANUAL | Live child-window and Steam validation |
| Orbit shell and dock | IMPLEMENTED, ATTENDED PENDING | Dock icon startup reconciliation and attended `START-005` pass; synthetic Settings icon fallback and boundary magnification correction are implemented, with `UI-008` and `UI-009` attended gates pending |
| Session shutdown guard | IMPLEMENTED, ATTENDED PENDING | `animate-shutdown` now confirms before `loginctl terminate-session`; deterministic and current-session live checks pass; attended cancel/confirm validation remains under `START-006` |
| Application lifecycle isolation | IMPLEMENTED | Orbit-launched applications now use independent user scopes and survive Orbit restarts; `START-007` passed with Zen and WezTerm |
| Orbit top panel | IMPLEMENTED, ATTENDED PENDING | Per-monitor transparent panel, XMB launcher, clock, tray, application title/actions, XWayland DBusMenu, native-Wayland AT-SPI, and honest fallback actions are implemented; `UI-010`, two-monitor `UI-011`, destructive fallback, GTK4, and Electron coverage gates remain |
| Focused-monitor XMB | IMPLEMENTED, ATTENDED PENDING | Focus ownership is implemented; current both-monitor confirmation remains `UI-002` |
| Overview / Alt+Tab | IMPLEMENTED, STARTUP OPEN | Overview focus and rapid-cycle behavior passed under `UI-006`; current runtime trigger installation is open under `START-004` |
| Dock-to-XMB transition | IMPLEMENTATION CORRECTED, ATTENDED PENDING | Morph background now overlaps the launcher handoff; contract/live/soak checks pass; attended continuity validation remains under `ORB-XMB-BLANKING` |
| Settings | IMPLEMENTED, FEATURE/ATTENDED OPEN | Ownership split and transactional display recovery are implemented; attended draft/recovery gates and scratchpad keybind/dock controls remain |
| Theme generator | IMPLEMENTED, ATTENDED PENDING | Deterministic adapter propagation passes; cross-toolkit live/reload/restart matrix remains `THEME-003` |
| Noctalia autostart removal | MANUAL | Implementation is recorded; current fresh-login process/unit inspection remains `START-002` |
| Reserved XMB prototype removal | MANUAL | Implementation is recorded; current client/unit inspection remains `START-003` |
| Input helper permissions | PASS | udev-identity allowlist, fresh-login startup, and attended Alt+Tab/Alt-release validation passed |
| Pointer focus policy | PASS | `[input].follow_mouse = 2` is persisted in Orbit settings and read by `hyprland.lua`; contract coverage and `hyprctl getoption input:follow_mouse` both report 2 |
| Runtime Alt+Tab binding startup | OPEN, REVALIDATION REQUIRED | Current direct live audit found no `TAB` binding despite an active service and current compositor signature; earlier fresh-login and 39/39 runs are historical. See `START-004` and `VQ-20260817-18` |
| Refactor pass | IN PROGRESS | Runtime shell state writer and Settings system-action/application-matching boundaries centralized; see [[Orbit - Refactor Backlog]] |

## Definition Of Done

An item is complete only when its contract tests pass and all applicable live, attended, hardware, or recovery gates are recorded in [[Orbit - Test Matrix]].

Orbit 1.0 is complete only when the full accepted vision in [[Orbit - Session Scratchpad]] is implemented and independently validated, stabilization gates are closed, and the Standard dotfiles, Orbit, and Wallpaper split in [[Orbit - Repository Boundaries]] is complete and independently reproducible.

## Next Session Checklist

- [ ] Revalidate fresh login with the user manager reused (`START-001`).
- [ ] Revalidate that `orbit-shell.service` is active and Noctalia is absent from the session graph (`START-002`).
- [ ] Confirm no `phleg-xmb-*` clients exist.
- [ ] Test startup and unlock wallpaper transitions.
- [ ] Test XMB focus ownership on both monitors.
- [x] Test overview release and rapid release.
- [ ] Capture failures in [[Orbit - Issues and Corrections]].

## Current Evidence

- `tests/orbit/run-all`: PASS, 50/50 registered deterministic requirements; `tests/orbit/validate-project.py`: PASS with zero errors and zero migration warnings. Evidence: `/tmp/opencode/orbit-cleanup-contract-3`.
- `tests/orbit/run-all --live`: 53/54 passed with a current matching compositor signature. `LIVE-001`, `LIVE-002`, and `LIVE-003` passed; only `START-004` failed because Hyprland reported zero `TAB` bindings. Evidence: `/tmp/opencode/orbit-cleanup-live-final`.
- One-minute smoke mode: PASS, 11 iterations, no failures, maximum Orbit RSS 459312 KiB (`soak-2026-08-17T18-36-58Z-1152385`). This is smoke evidence, not a release soak.
- `START-006`: shell/Python syntax checks, contract 35/35 (`2026-08-17T17-33-32Z-719334`), live 39/39 (`2026-08-17T17-33-32Z-719339`), and one-minute soak 10/10 (`soak-2026-08-17T17-33-32Z-719341`) passed. The attended gate remains `VQ-20260817-16`; no real logout or session mutation was performed.
- Input authorization: current udev allowlist/fail-closed contract and live checks passed, and attended fresh-login Alt+Tab/Alt-release behavior was confirmed. This supersedes the reverted ACL-only attempt.

## Historical Automated Evidence

The entries below preserve their accumulated audit order. Where they conflict with **Current Evidence**, they are historical only and do not determine current state.

- `tests/orbit/run-all`: PASS, 20 contract checks on 2026-08-17 at 11:13 UTC.
- `tests/orbit/run-all --live`: PASS, 24 checks on 2026-08-17 at 11:14 UTC; the terminal signature matched `hyprctl instances`.
- DES-20260817-04 selection/title model: deterministic contract and live shell checks pass; global-menu protocol activation remains blocked by `ORB-TOP-PANEL-GLOBAL-MENU`.
- `ORB-TOP-PANEL-GLOBAL-MENU` boundary correction: both panel instances now select by their own monitor; service restart and 42px layer inspection passed. Activation remains unvalidated and blocked by the missing registrar/menu handle.
- `UI-011` protocol feasibility check at 12:04 UTC: contract and live suites passed 20/20 and 24/24; the current signature matched `hyprctl instances`, but the session bus has no `com.canonical.AppMenu.Registrar` and all observed clients are native Wayland (`xwayland: false`). No bridge implementation was added without a usable protocol source.
- Appmenu packages installed for fixture investigation: `appmenu-qt5`, `appmenu-qt5-profile.d`, `dbusmenu-qt5`, and `libdbusmenu-tools`; no registrar service was provided, and the current session remains unchanged. The package README warns against global Qt5 platform activation outside Plasma/KDE.
- Existing-tool validation: `Simple-Appmenu-Server` commit `bb5f40fbe35fd18ddde7a0b97d89d76619bef995` built successfully after installing `xxd`; the registrar exported `com.canonical.AppMenu.Registrar`, and manual registration/retrieval of the `org.dbusmenu.test` `/MenuBar` fixture passed. Orbit still needs a real XWayland window association before UI-011 can be validated end to end.
- `UI-012` bridge first pass: `orbit-appmenu` resolves registered XWayland windows and DBusMenu layouts; Qt5 Designer returned seven top-level menu entries. Orbit popup rendering and leaf activation remain attended validation gates.
- `UI-012` attended validation passed: with Qt5 Designer focused, the Orbit top-panel application menu opened and `File > New...` activated successfully. Native Wayland applications remain outside this registrar-based path.
- `UI-013` first pass: native Qt6 Designer launched with `QT_ACCESSIBILITY=1` on Wayland registered on AT-SPI; `orbit-appmenu-atspi snapshot` returned its menu tree and the Orbit shell loaded the new AT-SPI path. Attended native-Wayland popup/action validation remains pending.
- `UI-013` attended rendering passed after correcting the AT-SPI/QML field mapping: Qt6 Designer now shows named menu entries and separators in the Orbit panel. Safe leaf-action activation remains to be confirmed.
- `UI-013` attended activation passed: Qt6 Designer's native Wayland menu opened from Orbit and `File > New...` successfully activated through AT-SPI.
- `ORB-TOP-PANEL-GLOBAL-MENU` correction: per-monitor menu selection now rejects stale remembered clients by address, and protocol-backed menus are reflected as available in the panel button state. Contract checks passed 22/22 and live checks passed 26/26 on 2026-08-17 at 13:35 UTC. Attended two-monitor interaction and destructive fallback actions remain pending.
- GTK4 coverage check: native Nautilus registered on AT-SPI but exposed no readable `MENU_BAR`; full GTK4 application-menu extraction is unavailable for this app and remains an honest fallback case.
- Electron coverage check: disposable native Obsidian Flatpak with `--force-accessibility` did not register a usable AT-SPI menu in this session; retain fallback behavior rather than claim broad Electron support.
- `UI-016` fallback validation passed: Nautilus displayed `Open new window`, `Close window`, and `Force quit application`; the fallback popup was resized to show all actions.
- `THEME-004` transparency contract implemented: `appearance.transparency.shell_opacity` is now 0.30 and drives the dock, dock/XMB morph, XMB, and Settings main surfaces; the top panel and popup/control surfaces remain unchanged. Contract suite passed 23/23 on 2026-08-17 at 13:49 UTC. Attended Hyprglass visual validation remains pending. The live suite reached 26/27 because unrelated `START-004` Alt+Tab binding validation failed; the session signature matched `hyprctl instances`.
- `OBS-20260817-03` root cause: the 2026-08-17 11:29:10 session interruption was initiated by the unguarded `SUPER + M` `animate-shutdown` path, which called `loginctl terminate-session`; Orbit only reported the resulting Wayland disconnect. The path now requires explicit confirmation and fails closed without `zenity`. `START-006` deterministic/live checks pass; attended validation remains pending.
- Orbit restart lifecycle correction: Orbit-launched applications inherited `orbit-shell.service` and were killed by its `KillMode=control-group`. `orbit-app-launch` now places launches in independent user scopes; fresh Zen and WezTerm windows survived repeated restarts with an unchanged Hyprland signature. `START-007` passed on 2026-08-17.
- `UI-011` fallback: application-name click now offers close-window and force-quit actions when no protocol menu handle exists; destructive attended click validation remains open.
- Historical `UI-011` monitor mapping correction: numeric Hyprland client monitor IDs are now resolved through monitor names; the correction run passed 18/18 plus 22/22 checks. Current totals are recorded above.
- `UI-011` menu placement correction: protocol menu opening now uses a `QsMenuAnchor` with screen-edge adjustment; latest shell reload emitted no menu-anchor warning.
- Dock icon startup reconciliation: implementation, automated coverage, and attended `START-005` validation pass on 2026-08-17.
- Historical input helper attempt: OPEN; the ACL-only mitigation regressed after logout/login and was reverted. It was superseded by the current passing udev-identity allowlist.
- Input helper permissions: `.config/orbit/input-devices.toml` allowlists the physical keyboard and Logitech receiver by udev serial; `orbit-input-state` requires `pyudev`, ignores non-keyboard/unknown devices, and fails closed without the allowlist. After logout/login, the service restarted correctly and the user confirmed Alt+Tab/Alt-release behavior; live validation passed 28/28.
- Historical runtime Alt+Tab binding startup: fresh-login validation passed with bindings present without manual restoration; the later direct live audit now requires `START-004` revalidation.
- Rapid Alt+Tab: duplicate-focus fix passed repeated attended rapid-cycle testing after shell reload.
- Pointer focus policy: `follow_mouse = 2` is stored in `.config/orbit/settings.toml`, loaded by the Hyprland Lua configuration, and currently observed at runtime with `hyprctl getoption input:follow_mouse -j` returning `int: 2`.
- Two-minute soak: PASS, 19 iterations, no failures.
- Soak maximum recorded Orbit RSS: approximately 481 MiB; investigate during the runtime snapshot/polling refactor.
- Runtime state writer refactor: `.local/lib/orbit-state` now provides shared locking and unique temporary-file replacement for shell visibility, XMB, overview, and cycle-request state. Contract suite passed 24/24 on 2026-08-17 at 13:56 UTC. No live or attended check was required because the change preserves the existing state format and only changes deterministic write mechanics.
- Overview state-machine refactor: `orbit-overview` now exclusively writes `overview-visible` as `status revision cycle_count`; QML consumes that file for visibility and cycle requests, and the obsolete `overview-cycle` file is removed. Contract passed 25/25, live passed 29/29, and the one-minute soak passed 11/11 iterations on 2026-08-17. QuickShell restarted without Orbit QML errors.
- Core runtime snapshot refactor: `HyprlandModel.qml` now owns the shared monitor, client, workspace, and active-workspace polling boundary consumed by the core shell models. Settings matching and external menu bridges retain their narrower polling boundaries. Contract passed 28/28 (`2026-08-17T15-25-12Z-3994500`), live passed 32/32 (`2026-08-17T15-25-18Z-3995303`), and one-minute soak passed 11/11 (`soak-2026-08-17T15-25-21Z-3994937`). `orbit-shell.service` restarted active with compositor signature `5c9377c15f85c50648f35ca5a213754f95b93ca0_1786975562_1510797335`; no new QML errors were logged.
- Runtime polling correction: `SettingsModel.qml` now consumes `HyprlandModel.qml` client updates for application matching instead of starting its own `hyprctl clients -j` poll. Contract 29/29 (`2026-08-17T15-35-34Z-4072076`), live 33/33 (`2026-08-17T15-35-34Z-4072141`), and one-minute soak 11/11 (`soak-2026-08-17T15-35-35Z-4072075`) passed; `orbit-shell.service` restarted active with the compositor signature matched.
- Monitor/workspace source correction: reserved workspace numbers now live beside each monitor role in `.config/orbit/settings.toml`; `orbit-monitor workspace-for-monitor` is the shared resolver used by `dynamic-app-workspaces`. Contract 29/29, live 33/33, and one-minute soak 10/10 passed on 2026-08-17 (`2026-08-17T15-39-26Z-4101010`, `2026-08-17T15-39-26Z-4101027`, `soak-2026-08-17T15-39-26Z-4101028`).
- Shared launch boundary correction: dock and XMB application launches now use the same `orbit-app-launch` independent user scope with captured monitor/workspace context, while `orbit-app-observe` records the desktop identity for later correlation. Contract 29/29 (`2026-08-17T15-32-16Z-4048673`), live 33/33 (`2026-08-17T15-32-17Z-4048738`), and one-minute soak 11/11 (`soak-2026-08-17T15-32-21Z-4048672`) passed. Disposable cross-surface attended launch validation remains `VQ-20260817-07`.
- Concurrent launch correlation correction: dock and XMB launch IDs now flow from `orbit-app-observe` through `orbit-app-launch`; the mapped client's address, PID, class, and title consume the exact pending record instead of the newest same-app record. Contract 30/30 (`2026-08-17T15-45-08Z-4147520`), live 34/34 (`2026-08-17T15-45-20Z-4149043`), and one-minute soak 11/11 (`soak-2026-08-17T15-45-27Z-4150028`) passed. The disposable two-record fixture passed; attended concurrent same-app validation remains `VQ-20260817-08`.
- Alt-release ownership refactor: the allowlisted `orbit-input-state` helper is now the only release event source; Orbit removes legacy Hyprland `Alt_L`/`Alt_R` release actions and retains only the `Alt+Tab` trigger. Contract passed 25/25 and live passed 29/29 after shell restart; attended rapid Alt-release validation remains the applicable manual gate.
- Overview cycle corrections: equal revisions are ignored, cycle revisions preserve selection, and the layer refocus pulse remains for floating-client transitions. `ORB-OVERVIEW-STATE-REFRESH` and `ORB-OVERVIEW-FLOATING-CYCLE` both passed attended standard-speed cycling with and without floating windows on 2026-08-17; contract 25/25 and live 29/29 pass.
- `START-006` worker validation: contract passed 25/25, live passed 29/29, and the one-minute soak passed 11/11 on 2026-08-17. A disposable fake-command fixture passed cancel and confirm ordering without invoking the real session termination path. The attended cancel/confirm gate is handed off as `VQ-20260817-01`.
- `ORB-XMB-BLANKING` correction: `dockMorphSurface` remains painted during the `dockHandoff` overlap instead of disappearing at the same boundary where the launcher surface becomes visible. Contract passed 25/25 (`2026-08-17T14-53-19Z-3716300`), live passed 29/29 (`2026-08-17T14-53-25Z-3716878`), and one-minute soak passed 11/11 (`soak-2026-08-17T14-53-29Z-3717902`). Attended visual continuity and interruption checks remain under `VQ-20260817-02`.
- `ORB-APP-FOCUS-RACE` correction: dock and XMB launches capture the initiating monitor/workspace and relocate a newly mapped matching client after delayed startup. Contract passed 25/25 (`2026-08-17T14-58-44Z-3762377`), the service restarted cleanly, live passed 29/29 (`2026-08-17T15-00-42Z-3779691`), and one-minute soak passed 11/11 (`soak-2026-08-17T14-58-50Z-3763514`). The controlled focus-change fixture remains attended as `VQ-20260817-03`.
- `ORB-APP-EXEC-FIELDS` correction: shared `orbit-app-launch` normalization removes no-file Desktop Entry substitutions such as `%U`/`%F` before invoking the application and preserves `%%`. Contract passed 26/26 (`2026-08-17T15-04-00Z-3805284`), live passed 30/30 (`2026-08-17T15-04-03Z-3805765`), and one-minute soak passed 11/11 (`soak-2026-08-17T15-04-11Z-3806901`). Attended Nautilus/Zen relaunch confirmation remains `VQ-20260817-04`.
- `ORB-DOCK-SETTINGS-ICON` correction: synthetic `orbit-settings` dock entries now resolve the standard `settings-symbolic` icon through `ApplicationModel.qml`. Contract passed 26/26 (`2026-08-17T15-07-52Z-3836324`), live passed 30/30 (`2026-08-17T15-07-54Z-3836727`), and one-minute soak passed 11/11 (`soak-2026-08-17T15-07-57Z-3837506`). `orbit-shell.service` restarted active; attended dock inspection remains `VQ-20260817-05`.
- `START-008` duplicate-process/startup-readiness coverage: the contract suite now asserts Orbit waits for bounded Wayland/Hyprland prerequisites, fails closed when they never arrive, and invokes QuickShell with `--no-duplicate`. Contract passed 27/27 (`2026-08-17T15-12-08Z-3873183`), live passed 31/31 (`2026-08-17T15-12-08Z-3873248`), and one-minute soak passed 11/11 (`soak-2026-08-17T15-12-13Z-3873178`).
- `ORB-BLANK-WORKSPACE-RESOLUTION` correction: `blank-special-workspaces` now resolves each connected output through `orbit-monitor workspace-for-monitor` rather than a connector-specific case statement. Contract passed 30/30 (`2026-08-17T15-45-15Z-4148341`) and live passed 34/34 (`2026-08-17T15-45-19Z-4148845`); attended two-monitor enter/hold/restore validation remains queued as `START-009`.
- `ORB-ALT-RAPID-CYCLE` backlog closure rechecked: contract passed 27/27 (`2026-08-17T15-15-02Z-3897919`), live passed 31/31 (`2026-08-17T15-15-05Z-3898317`), and one-minute soak passed 11/11 (`soak-2026-08-17T15-15-12Z-3899641`); attended rapid-cycle evidence remains the existing 2026-08-17 PASS recorded under `UI-006`.
- `ORB-XMB-DOCK-FOCUS` correction: the dock morph now releases `dockMorphing` at handoff, allowing the existing exclusive keyboard-focus guard to activate for dock-launched XMB. Contract passed 28/28 (`2026-08-17T15-21-01Z-3954782`), live passed 32/32 (`2026-08-17T15-21-01Z-3954791`), and one-minute soak passed 10/10 (`soak-2026-08-17T15-21-01Z-3954801`). `orbit-shell.service` reloaded active with the current compositor signature; attended keyboard navigation and Escape recovery remain `VQ-20260817-17`.
- Current `ORB-APP-CONCURRENT-CORRELATION` evidence supersedes the initial worker run: contract 30/30 (`2026-08-17T15-48-16Z-4171928`), live 34/34 (`2026-08-17T15-48-17Z-4171926`), and one-minute soak 11/11 (`soak-2026-08-17T15-48-25Z-4173026`) pass after launch-ID inheritance through `/proc/<pid>/environ`; attended validation remains `VQ-20260817-08`.
- `ORB-STEAM-TOAST` correction: floating Steam menus/toasts now correlate to the visible non-floating Steam client without requiring PID equality. Contract 30/30 (`2026-08-17T15-54-08Z-17457`), live 34/34 (`2026-08-17T15-54-08Z-17526`), and one-minute soak 11/11 (`soak-2026-08-17T15-54-13Z-17455`) pass; attended controlled toast validation remains `VQ-20260817-09`.
- `ORB-APP-POLICY-FORMATS` correction: legacy `[[policy]]` and modern `[[rule]]` entries now normalize through one matcher while preserving modern priority precedence. Contract 30/30 (`2026-08-17T15-58-47Z-49050`), live 34/34 (`2026-08-17T15-58-51Z-49525`), and one-minute soak 10/10 (`soak-2026-08-17T15-58-51Z-49526`) pass; no manual gate remains.
 - `START-006` worker rerun: contract 30/30 (`2026-08-17T16-17-04Z-191390`), live 34/34 (`2026-08-17T16-17-04Z-191423`), and one-minute soak 10/10 (`soak-2026-08-17T16-17-05Z-191475`) pass. `zenity` is installed, `orbit-shell.service` is active, and the current compositor signature matches `hyprctl instances`; no real logout was invoked. The attended cancel and explicitly approved confirm/re-login gate remains `VQ-20260817-01`.
 - `ORB-SETTINGS-MODEL-BOUNDARIES` application-matching split: `SettingsApplicationMatching.qml` now owns the shared client projection, candidate discovery timers, and match actions; `SettingsModel.qml` keeps compatibility aliases/wrappers. Contract 30/30 (`2026-08-17T16-07-40Z-117230`), live 34/34 (`2026-08-17T16-07-42Z-117595`), and one-minute soak 10/10 (`2026-08-17T16-07-46Z-118244`) pass. Draft lifecycle remains a separate parent-owned boundary.
- `ORB-SETTINGS-MODEL-BOUNDARIES` completed: `SettingsDraftLifecycle.qml` now owns staged draft state, cancel/apply confirmation, timeout cancellation, and apply commit while `SettingsModel.qml` preserves its public API. Contract 30/30 (`2026-08-17T16-13-41Z-163889`), live 34/34 (`2026-08-17T16-13-41Z-163970`), and one-minute soak 10/10 (`soak-2026-08-17T16-13-42Z-163888`) pass; the service restarted active with no new Orbit QML errors.
- `ORB-SETTINGS-PERSISTENCE-BOUNDARY` completed: `.local/lib/orbit_settings_persistence.py` owns generic settings atomic writes, TOML/JSON reads, and TOML serialization while the CLI imports the boundary unchanged. Contract 31/31 (`2026-08-17T16-28-50Z-272290`), live 35/35 (`2026-08-17T16-28-54Z-272765`), and one-minute soak 10/10 (`soak-2026-08-17T16-28-54Z-272766`) pass; no manual gate applies.
- `START-006` worker refresh: contract 34/34 (`2026-08-17T16-59-05Z-480469`), item-specific live `START-006` passed within 37/38 (`2026-08-17T16-59-08Z-480899`), and one-minute soak passed 10/10 (`soak-2026-08-17T16-59-08Z-480908`). `zenity` is installed, Orbit is active, and the compositor signature matches; unrelated live `START-004` failed. No real logout or session termination was invoked. Attended cancel and explicitly approved confirm/re-login remain queued as `VQ-20260817-13`.
- `START-006` fresh worker validation: shell syntax and Python compilation passed; contract 35/35 (`2026-08-17T17-18-45Z-617766`), item-specific `START-006` remained PASS in live 38/39 overall (unrelated `START-004` Alt+Tab assertion failed; `2026-08-17T17-18-49Z-618234`), and one-minute soak passed 11/11 (`soak-2026-08-17T17-18-56Z-619246`). `/usr/bin/zenity`, active `orbit-shell.service`, and the matching compositor signature were confirmed. Safe cancel and missing-`zenity` fixtures invoked no `loginctl`; no real logout was attempted. Attended cancel and explicitly approved confirm/re-login remain queued as `VQ-20260817-14`.
- Historical `ORB-STARTUP-BINDINGS` hardening evidence: `.local/bin/orbit-shell` waits up to 60 bounded attempts for the runtime Alt+Tab binding and refuses to start QuickShell if installation never succeeds. After restart, contract 35/35 (`2026-08-17T17-24-08Z-654311`), live 39/39 including `START-004` (`2026-08-17T17-24-13Z-655070`), and one-minute soak 11/11 (`soak-2026-08-17T17-24-16Z-654693`) passed. The later direct live audit found no `TAB` binding, so this run no longer establishes current PASS.
- `START-006` fresh worker rerun: syntax/Python compilation passed; contract 35/35 (`2026-08-17T17-28-30Z-684233`), live 39/39 (`2026-08-17T17-28-30Z-684296`), and one-minute soak 11/11 (`soak-2026-08-17T17-28-35Z-684232`) passed. `/usr/bin/zenity`, active `orbit-shell.service`, and the matching compositor signature were confirmed. No real logout or session mutation was performed; attended cancel and explicitly approved confirm/re-login remain queued as `VQ-20260817-15`.
- `START-006` current worker evidence: shell/Python syntax checks passed; contract 35/35 (`2026-08-17T17-33-32Z-719334`), live 39/39 (`2026-08-17T17-33-32Z-719339`), and one-minute soak 10/10 (`soak-2026-08-17T17-33-32Z-719341`) passed. `/usr/bin/zenity`, active `orbit-shell.service`, and the matching compositor signature were confirmed. Disposable cancel and missing-`zenity` fixtures returned safely without invoking `loginctl`; no real logout or session mutation was performed. Attended cancel and explicitly approved confirm/re-login remain queued as `VQ-20260817-16`.
